[project]
name = "unitree_webrtc_connect_battlebang"
version = "2.1.0"
description = "Connect to the Unitree Go2 and G1 with WebRTC"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "legion1581", email = "legion1581@gmail.com" }
]

dependencies = [
    "unitree-webrtc-core-rs",
    "aiortc>=1.9.0",
    "pycryptodome",
    "requests",
    "wasmtime",
    "flask-socketio",
    "lz4",
    "numpy",
    "packaging",
    "sounddevice",
    "pyaudio",
    "pydub",
    "opencv-python"
]

[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["unitree_webrtc_connect*"]
exclude = [
    "tests",
    "tests.*",
    "docs",
    "docs.*",
    "examples",
    "examples.*",
    "__pycache__",
    "__pycache__.*",
    "*.egg-info",
    "*.egg-info.*",
]

[tool.setuptools]
# include data files as listed in MANIFEST.in
include-package-data = true

[tool.uv.sources]
unitree-webrtc-core-rs = { path = "rust-core" }
